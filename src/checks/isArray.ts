/**
 * Проверяет, является ли переданное значение массивом.
 * Это простое перенаправление на встроенный метод `Array.isArray`.
 *
 * @param value - Значение, которое нужно проверить.
 * @returns Возвращает `true`, если значение является массивом, иначе `false`.
 */
export const isArray = Array.isArray;
