# Philosophy

  ```ts
import { _ch } from "./checks"; // Проверки на различные структуры данных
import { _t } from "./transform"; // Трансформации с разными структурами данных (например, мутации, агрегации)
import { _h } from "./helpers"; // Различные помощники или утилиты
import { _m } from "./mechanics"; // Различне механики. Не изменяющие структуры данных и не производящие проверки
import { _c } from "./classes"; // Базовые 5-10 Classes от которых можно будет наследоваться и расширять функционала под любой проект
import { _p } from "./patterns"; // Готовые решения на основе паттернов проектирования, например adapter, builder и другие

Пример использования _ch.isFalse(payload)
Пример использования _t.isFalse(false)
Пример использования _h.unDefWithDefaultField(payload) 
Пример использования _m.debounce(paylod)
Пример использования _c. // 
Пример использования _p.toCamelCaseCollection(payload)
  ```

  ```scss
examples
  ```