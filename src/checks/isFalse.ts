/**
 * Проверяет, является ли переданное значение `false`.
 *
 * @param v - Значение, которое нужно проверить.
 * @returns Возвращает `true`, если значение строго равно `false`, иначе `false`.
 */
export const isFalse = (v: any): boolean => v === false;
